---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app sabnzbd4k
  namespace: downloads
spec:
  interval: 30m
  chart:
    spec:
      # renovate: registryUrl=https://bjw-s.github.io/helm-charts/
      chart: app-template
      version: 3.7.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      interval: 30m

  install:
    remediation:
      retries: 5
  upgrade:
    remediation:
      retries: 5
  dependsOn:
    - name: longhorn
      namespace: storage
    - name: volsync
      namespace: volsync-system
  values:

    defaultPodOptions:
      securityContext:
        runAsUser: 568
        runAsGroup: 568
        fsGroup: 568
        fsGroupChangePolicy: OnRootMismatch
        supplementalGroups:
          - 65539

    controllers:
      sabnzbd4k:

        annotations:
          reloader.stakater.com/auto: "true"

        containers:
          sabnzbd4k:
            image:
              repository: ghcr.io/onedr0p/sabnzbd
              tag: 4.4.1@sha256:78368fe35996a3f08f98fadaf8d336a133fe77faa191c873fda0e7494d0ac7cb
            env:
              TZ: "America/New_York"
              SABNZBD__PORT: &port 8080

            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
              startup:
                enabled: true
                spec:
                  failureThreshold: 30
                  periodSeconds: 5

    service:
      sabnzbd4k:
        controller: *app
        ports:
          http:
            port: *port

    ingress:
      sabnzbd4k:
        enabled: false
        className: "nginx"
        hosts:
          - host: &host "sabnzbd4k.${INTERNAL_DOMAIN}"
            paths:
              - path: /
                service:
                  identifier: sabnzbd
                  port: http
        tls:
          - hosts:
              - *host
            secretName: sabnzbd4k.tls


    persistence:

      config:
        enabled: true
        existingClaim: *app

      downloads:
        enabled: true
        existingClaim: sabnzbd4k-downloads-v1
        globalMounts:
          - path: /downloads

      temp:
        type: nfs
        path: /mnt/volume01/sabnzbd4k
        globalMounts:
          - path: /temp_dir

    resources:
      requests:
        memory: 250Mi
        cpu: 500m
      limits:
        memory: 16384Mi

  valuesFrom:
    - kind: Secret
      name: *app
      valuesKey: NAS4_ADDR
      targetPath: persistence.temp.server
